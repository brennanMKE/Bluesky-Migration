# 0014 — Module 4: #Preview canvas not validated in Xcode

| | |
|---|---|
| **Status** | resolved |
| **Module** | BlueskyUI |
| **Platform** | All |
| **First seen** | 2026-04-29 |

## Description

All `BlueskyUI` components have `#Preview` blocks but they have never been opened in Xcode Canvas and visually verified. Layout issues, missing assets, or theme misapplication could be lurking.

## Steps to reproduce

1. Open the `BlueskyKit` package in Xcode.
2. Open each view file in `Sources/BlueskyUI/` and enable Canvas (Editor → Canvas).
3. Inspect each `#Preview` in all lighting/size variants.

## Expected behavior

All component previews render correctly in light, dark, and dim themes across compact and regular size classes.

## Actual behavior

Not validated — previews have never been opened in Xcode Canvas.

## Notes

Components to check: `AvatarView`, `RichTextView`, `PostEmbedView`, `PostCard`, `FeedCard`, `ListCard`, `BlueskyButtonStyle` variants, `BlueskyTextField`, `ToastView`.

## Incomplete reason

Cannot be validated by code analysis alone — requires running the app in Xcode against a live device or simulator. Marked incomplete until live testing is performed.
