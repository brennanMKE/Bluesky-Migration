# 0207 — Composer crashes after a successful thread post (stale index in `threadPostSection` binding)

| | |
|---|---|
| **Status** | resolved |
| **Module** | BlueskyComposer |
| **Platform** | All |
| **First seen** | 2026-06-11 |
| **Closed** | 2026-06-11 |
| **Commit** | BlueskyKit `a82eaf8` |

## Description

Submitting a thread (root + at least one additional post) crashes the app
immediately after the submit succeeds. The posts land on the PDS correctly —
the crash happens client-side while the composer tears down.

`ComposerSheet.threadPostSection(index:)` builds the extra thread editors with
an unguarded index subscript captured inside a `Binding`:

```swift
// BlueskyKit/Sources/BlueskyComposer/ComposerSheet.swift:958-961
TextEditor(text: Binding(
    get: { viewModel.additionalPosts[index] },
    set: { viewModel.additionalPosts[index] = $0 }
))
```

(`CharProgressRing` at line 968 subscripts the same way.) The thread rows come
from `ForEach(viewModel.additionalPosts.indices, id: \.self)`, so the binding
captures a raw `Int`. On a successful submit `ComposerViewModel` resets
`additionalPosts = []` (ComposerViewModel.swift, post-submit reset); SwiftUI
then re-evaluates the still-mounted `TextEditor`'s binding with the stale
index and `Array._checkSubscript` traps:

```
exception: EXC_BREAKPOINT (SIGTRAP)
libswiftCore  Array._checkSubscript(_:wasNativeTypeChecked:)
libswiftCore  Array.subscript.getter
Bluesky Beta  closure #1 in ... in ComposerSheet.threadPostSection(index:)
SwiftUICore   Binding.ScopedLocation.update() …
```

Full crash report: [crash-after-thread-submit.ips](0207/crash-after-thread-submit.ips).
Composer state just before the submit: [thread-composer-before-submit.png](0207/thread-composer-before-submit.png).

Found during the #0064 gate re-run
(`ComposerGateUITests/test04ThreadPostWithLinkCard`, iPhone 17 simulator,
iOS 26.5, 2026-06-11). The same reset-while-mounted pattern would also fire
on any other path that clears or shrinks `additionalPosts` while the sheet is
on screen (e.g. removing a middle post repositions later indices —
`removePost(at:)` is guarded in the view model but the *bindings* are not).

## Steps to reproduce

1. Sign in, open the composer.
2. Type root text, tap **Add to thread**, type text in the second editor.
3. Tap **Post** and wait for the submit to succeed.

## Expected behavior

Both posts are created (root + reply) and the composer dismisses cleanly,
returning to the feed.

## Actual behavior

Both posts are created on the server, then the app crashes
(`Fatal error: Index out of range`) during composer dismissal.

## Notes

- Server evidence that the submit itself works (from the #0064 re-run):
  root `…/app.bsky.feed.post/3mnyz5qcqlr26` carried the
  `app.bsky.embed.external` card and the reply `…/3mnyz5qzfzr2v` had correct
  `reply.root`/`reply.parent` refs before cleanup deleted them.
- Fix direction: make the row bindings index-safe (e.g.
  `get: { index < viewModel.additionalPosts.count ? viewModel.additionalPosts[index] : "" }`,
  guarded `set:`), or iterate identifiable elements instead of raw indices.
- Blocks the thread leg of the #0064 Module 12 iOS composer gate.

## Root cause

`threadPostSection(index:)` built each extra thread editor from
`ForEach(viewModel.additionalPosts.indices, id: \.self)`, capturing the raw
`Int` index inside the `TextEditor`'s `Binding` get/set closures (and in the
`CharProgressRing` character-count subscript). On a successful submit,
`ComposerViewModel.post()` deliberately resets `additionalPosts = []` before
the sheet dismisses (the reset prevents a final frame from showing the
just-posted content). SwiftUI then re-evaluates the still-mounted
`TextEditor`'s binding (`Binding.ScopedLocation.update()`) with the stale
index against the now-empty array, and `Array._checkSubscript` traps:
`Fatal error: Index out of range`.

## Fix

Made the row bindings total instead of re-sequencing reset vs. dismissal
(BlueskyKit `a82eaf8`, `Sources/BlueskyComposer/ComposerSheet.swift`):

- Added an index-safe helper `threadPostText(at:)` that returns `""` when the
  index is out of range; the `TextEditor` binding's `get` and the
  `CharProgressRing` count both read through it.
- The binding's `set` now guards `viewModel.additionalPosts.indices.contains(index)`
  and no-ops when the row no longer exists.

Sequencing rationale: the post-submit reset-then-`dismiss()` order in
`ComposerViewModel.post()` / the Post toolbar button is intentional and kept —
deferring the reset until after the dismissal animation would reintroduce the
flash of just-posted content the reset exists to prevent, and would still
leave the bindings fragile against any other path that shrinks the array
while the sheet is on screen (e.g. removing a middle post). Total bindings
fix the class of bug, not just this instance.

Sibling bindings in the same file were audited: the image-grid alt-text
binding is id-based with a guarded set, the video alt binding is
optional-safe, and the alert/threadgate toggle bindings subscript nothing —
only the thread rows had the hazard.

No unit test added: the unguarded subscripts lived in the View's `Binding`
closures, and the trap fires from SwiftUI's render-pass binding update on a
dismissing sheet — pure SwiftUI render timing, not reachable from the
view-model API (`removePost(at:)` was already guarded). Coverage comes from
the live UI test below.

## Verification

All commands run 2026-06-11, foreground.

- `swift test` in `/Users/brennan/Developer/Bluesky/BlueskyKit` —
  `Test run with 142 tests in 27 suites passed`.
- App built for the iOS Simulator (default signing) and installed on the
  booted iPhone 17 sim (`25FCA843-F98D-48C8-A679-CFABCBA96A78`):
  `xcodebuild build -workspace Bluesky.xcworkspace -scheme "Bluesky (Beta)" -destination id=25FCA843-F98D-48C8-A679-CFABCBA96A78` →
  `** BUILD SUCCEEDED **`, then `xcrun simctl install` of
  `Debug-iphonesimulator/Bluesky Beta.app`.
- Live re-run of the exact failing leg:
  `xcodebuild test -workspace Bluesky.xcworkspace -scheme "Bluesky (Beta)" -destination id=25FCA843-F98D-48C8-A679-CFABCBA96A78 -only-testing:Bluesky-SwiftUIUITests/ComposerGateUITests/test04ThreadPostWithLinkCard BLUESKY_TEST_HANDLE=rnmigration.bsky.social BLUESKY_TEST_PASSWORD=placeholder` →
  `Test Case '-[Bluesky_SwiftUIUITests.ComposerGateUITests test04ThreadPostWithLinkCard]' passed (18.498 seconds)` — **no crash**.
- Server evidence (public AppView `getAuthorFeed`, before cleanup): feed
  count 3 — root `at://did:plc:jcphjuamknfm2mh3p262uvr6/app.bsky.feed.post/3mnyzsen2pv2v`
  with `embed.$type = "app.bsky.embed.external"`,
  `external.uri = "https://github.com"`; reply `…/3mnyzsfi4y42w` with
  `reply.root` = `reply.parent` = the root uri.
- Cleanup: `ComposerGateCleanupTests/testDeleteMarkerPosts` passed
  (33.356 seconds); `getAuthorFeed` afterwards: `feed count: 1` —
  `at://did:plc:jcphjuamknfm2mh3p262uvr6/app.bsky.feed.post/3mmnqjqdzjs2s`
  (exactly the baseline post).

## Files changed

- `BlueskyKit/Sources/BlueskyComposer/ComposerSheet.swift` — total bindings
  in `threadPostSection(index:)` + new `threadPostText(at:)` helper.

## Gotchas

- `ForEach(array.indices, id: \.self)` + index-capturing `Binding` is unsafe
  against any mutation that shrinks the array while rows are mounted; prefer
  identifiable elements or total bindings anywhere this pattern recurs.
