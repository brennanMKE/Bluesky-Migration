# 0192 — macOS UI test suite: direct messages (conversation list and thread)

| | |
|---|---|
| **Status** | open |
| **Module** | Tests / BlueskyMessages |
| **Platform** | macOS |
| **First seen** | 2026-05-25 |

## Description

macOS mirror of #0181. Run/adapt the direct-messages coverage on **macOS**: the conversation list loads, opening a conversation pushes the message thread, and the thread renders bubbles + a compose field. Account for the macOS shell (the DM UI may be a column/split layout on the Mac).

## Scope

- Verify on `platform=macOS`, reusing `conversation-list-screen` / `conversation-cell` / `conversation-thread-screen` / `message-bubble` / `message-compose-bar` identifiers from #0181.
- The test account's DM inbox may be empty, so conversation/thread checks should skip cleanly (as in the iOS suite); the list-loads / empty-state checks must genuinely run.

## Verification

`xcodebuild test -scheme "Bluesky (Beta)" -testPlan Screenshots -destination 'platform=macOS' …` runs the macOS messages tests green (most likely list-loads passing + conversation tests skipping on an empty inbox).

## Notes

- Mirrors #0181. Blocked by #0186.
