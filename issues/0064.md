# 0064 — Module 12 gate (iOS): composer live validation on iPhone

| | |
|---|---|
| **Status** | open |
| **Module** | BlueskyComposer |
| **Platform** | iOS |
| **First seen** | 2026-05-05 |

## Description

Once the iOS build is restored (#0061), the Module 12 composer gate must be re-validated on a real iPhone. iOS uses a different image picker (`PHPickerViewController` via `PhotosPicker` / `PhotosPickerItem`) and different keyboard / safe-area / rubber-band scroll behavior than macOS, and several composer code paths are gated `#if os(iOS)` — they have not been exercised since #0034 landed.

This issue split off from #0035, which now focuses on the macOS gate.

## Steps to reproduce

1. Resolve #0061 so the iOS build is green.
2. Install the app on a registered iPhone (or run in iOS Simulator) and sign in.
3. Run the live checks below.

## Live checks (must all pass)

- **Plain-text post.** Type a short post, submit, confirm it appears in your feed.
- **Image post (PHPicker).** Tap the image button, pick 1–4 images via PHPicker, optionally add alt text, submit, confirm the post renders with all images in the feed and embeds round-trip after a refresh.
- **Image post — alt text.** With one image attached, open the alt-text editor, save, submit, confirm `accessibilityLabel` is preserved on the post embed.
- **Video post.** Tap the video picker, select a video, confirm it uploads, submit, confirm the post renders with the video.
- **@mention autocomplete.** Type `@` plus a few characters; confirm suggestion overlay appears; pick a suggestion; confirm the posted mention is a live profile link.
- **Link card preview.** Paste a URL; confirm the OpenGraph card resolves with title/description/thumbnail; submit; confirm the card embed appears in the feed.
- **Thread composer.** Add a second post to make a thread; submit; confirm both posts appear with the second as a reply to the first.
- **Draft persistence (per-context).** Type without posting; dismiss; reopen the same composer (root vs. reply-to); confirm the draft restored matches the context. Drafts are keyed per-`replyTo.uri`.
- **Draft clears after successful post.** Post successfully; immediately reopen the composer; confirm the text field is empty.
- **Keyboard / safe area.** Confirm the input bar tracks the keyboard, the character counter stays visible, and the toolbar is reachable above the home indicator.

## Expected behavior

All ten checks pass. iOS-specific paths (`PhotosPicker` / `PhotosPickerItem`, keyboard avoidance, safe-area insets) behave correctly without regressions from the macOS-focused work.

## Actual behavior

Cannot be validated until #0061 is resolved.

## Notes

- Code under iOS gates touched recently: `BlueskyKit/Sources/BlueskyComposer/ComposerViewModel.swift` (`attachVideo`, line ~182), `BlueskyKit/Sources/BlueskyComposer/ComposerSheet.swift` (`selectedVideo`, line ~20).
- Reference for iOS-specific patterns: `Bluesky-ReactNative/src/view/com/composer/`.
- Reference commit for macOS image picker work that may have shifted iOS-side code: BlueskyKit `c826300`.
- Blocked by: #0061 (iOS build audit).
- Related: #0030 (push notifications) — also blocked on the iOS build.

## Live testing observations (2026-06-10)

Gate exercised on the booted iPhone 17 simulator (iOS 26.5, `Bluesky (Beta)`,
live `rnmigration.bsky.social` session) via a new XCUITest suite,
`Bluesky-SwiftUIUITests/ComposerGateUITests.swift`, plus a new
`deleteTestPosts` cleanup intent in `UITestNavigator` (both committed to the
app repo under `#0064`). Mutation budget held to one text post and one
attempted image post, both marker-prefixed `UITest-0064`; cleanup verified
via the public AppView (`getAuthorFeed` shows only the baseline post) and
on-device ([after-cleanup](0064/after-cleanup.png)).

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | Plain-text post | **PASS** — posted, visible on own profile, server record verified, deleted afterwards | [text-post-in-profile](0064/text-post-in-profile.png) |
| 2 | Image post (PHPicker) | **FAIL at submit** — picker, attach, and grid all work, but the PDS rejects the record (`$.record.embed.images[0].image` expects a blob value type) → **#0197** | [image-attached](0064/image-attached.png), [image-post-failed](0064/image-post-failed.png) |
| 3 | Image alt text | UI editor works (context menu → editor → Done); server-side `accessibilityLabel` round-trip **blocked by #0197** | [image-alt-editor](0064/image-alt-editor.png) |
| 4 | Video post | Picker + attach **PASS** (preview, alt/captions affordances); upload/submit **blocked by #0197** (same `BlobRef` encode path) and not exercised to limit live mutations | [video-attached](0064/video-attached.png) |
| 5 | @mention autocomplete | **PASS** — overlay appears, selection inserts handle, posted record carries a live `richtext.facet#mention` (verified via public AppView). Minor defect: overlay lingers after completion → **#0198** | [mention-suggestions](0064/mention-suggestions.png) |
| 6 | Link card preview | Preview **PASS** (title, description, thumbnail, host all resolved for github.com); **submit with card FAILS** (`$.record.embed.external.thumb`) → **#0197**. The gate post was submitted after removing the URL to preserve the mutation budget | [link-card](0064/link-card.png) |
| 7 | Thread composer | UI **PASS** — second editor mounts with its own character ring and accepts input; thread *submission* not validated (would exceed the mutation budget) | [thread-composer](0064/thread-composer.png) |
| 8 | Draft persistence (per-context) | **Spec drift**: the per-`replyTo.uri` auto-restore described above was deliberately removed in #0151. The current design **PASSES**: Cancel offers Save Draft, a fresh open is empty, the Drafts sheet restores the saved draft, swipe-to-delete works | [drafts-sheet](0064/drafts-sheet.png), [draft-restored](0064/draft-restored.png) |
| 9 | Draft clears after successful post | **PASS** — composer reopened empty immediately after the text post | [empty-after-post](0064/empty-after-post.png) |
| 10 | Keyboard / safe area | **PASS** — with the keyboard up the character counter and Post button stay visible and hittable; toolbar reachable | [keyboard-up](0064/keyboard-up.png) |

**Net:** 6 of 10 checks pass outright; image post, alt-text round-trip, video
post, and the link-card submit are all blocked by a single new defect —
**#0197** (`BlobRef` encodes without `$type: "blob"`, so every blob-carrying
record is rejected by the PDS). Re-run checks 2–4 and the submit half of 6
once #0197 is fixed. #0198 (lingering mention overlay) is cosmetic and does
not block this gate.

Note: this issue's "Blocked by #0061" framing is stale — the iOS build is
green and the app runs signed-in on the simulator; #0034's features (video,
link card, thread, drafts) are implemented and were exercisable.
