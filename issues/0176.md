# 0176 — UI test suite: home feed (load, rendering, and post interactions)

| | |
|---|---|
| **Status** | resolved |
| **Module** | Tests / BlueskyFeed |
| **Platform** | All |
| **First seen** | 2026-05-24 |
| **Closed** | 2026-05-24 |
| **Commit** | f54bf2e |

## Description

The home feed (`FeedView` / `FeedStore`) is the first screen a user sees and the highest-traffic path in the app. Feed rendering bugs have shipped repeatedly — broken dark mode (#0003), duplicate view models (#0049), stale cache (#0048), action buttons producing no state change (#0041, #0053). None were caught by automated tests. This suite locks in feed loading, post-cell rendering, and interaction button presence so regressions surface on the first CI run that breaks them.

Depends on #0175 (foundation).

## Regressions this suite would have caught

- **#0003** — Feed list ignored dark mode. A test asserting the feed background color matches the expected theme value would have caught it.
- **#0041 / #0053** — Like state stale after tap; like state doesn't persist. A test tapping the like button and asserting the button's accessibility value flips would catch the local-state regression. Persistence requires a relaunch cycle.
- **#0048** — Feed cache not used when offline. A test launching offline (block network via `XCUIApplication.launchEnvironment`) and asserting cached posts are visible would have caught it.
- **#0049** — `FeedView` task fired twice, creating duplicate view models. A test asserting post count is not doubled after a tab switch would have caught the duplication.

## Test cases

All tests use the #0175 harness. `launch(script: [.selectTab(.home), .waitForFeedLoad(minPosts: 1)])`.

### Feed loads and renders posts

- **At least one post cell is visible after launch.** Assert `app.cells["post-cell"].firstMatch.exists`.
- **Post cell contains expected elements.** Assert the first cell contains an avatar, display name, handle, post text, and action row (`post-action-like`, `post-action-repost`, `post-action-reply`, `post-action-share`).
- **No duplicate cells.** Assert `app.cells["post-cell"].count` is the same before and after switching away to Search and back to Home.

### Post interactions

- **Like button tap changes accessibility value.** Tap `post-action-like`. Assert its `accessibilityValue` (e.g. `"liked"` vs `"not liked"`) changes within 1 s.
- **Reply button navigates to composer or thread.** Tap `post-action-reply` on first post. Assert either a sheet appears or `ThreadView` pushes (`thread-view` accessibility identifier exists).

### Screenshot

- `screenshot(screen: "home-feed")` — captured after `waitForFeedLoad`.
- `screenshot(screen: "home-feed-dark")` — repeat with dark mode launch argument.

## Notes

- Accessibility identifiers (`post-cell`, `post-action-like`, etc.) need to be added to `PostCardView` and its action row as part of implementing this suite — the identifiers are the coupling surface between tests and views.
- Feed loading requires a live network connection. Mark tests that need the network with a custom tag so they can be excluded from offline CI runs.
- Like persistence across relaunch is a separate concern from local state flip; test them separately.

## Root cause

Not a bug — a coverage gap. The home feed (the app's highest-traffic screen)
had no automated UI coverage, so the rendering and action-button regressions
listed above all shipped undetected. This issue adds the suite that locks in
feed loading, post-cell rendering, and interaction-button presence/state.

## Fix

- **BlueskyKit (sanctioned accessibility-identifier exception):**
  - `PostCard` (BlueskyUI): tagged the card root `post-cell`
    (`.accessibilityElement(children: .contain)` so nested buttons stay
    addressable) and gave each action button an identifier
    (`post-action-reply` / `-repost` / `-like` / `-bookmark` / `-share`).
    The like and repost buttons publish an accessibility *value*
    (`liked`/`not liked`, `reposted`/`not reposted`) via a small
    `OptionalAccessibilityValue` modifier so a tap-and-assert test can verify
    the local-state toggle.
  - `ThreadView` (BlueskyFeed): tagged the thread list `thread-view` so the
    reply-navigation test can confirm a push.
- **Bluesky-SwiftUI:** new `HomeFeedUITests` test class (7 methods) built on
  the #0175 `BlueskyUITestHarness`. No new `UITestIntent` cases were needed —
  the existing `selectTab` + `waitForFeedLoad` intents plus `XCUIElement`
  interactions cover everything. Tests `XCTSkip` when
  `credentialsFromEnvironment()` is `nil`.

Test methods:

| Test | Asserts |
|------|---------|
| `testFeedLoadsAtLeastOnePostCell` | a `post-cell` exists after `waitForFeedLoad` |
| `testPostCellContainsActionRow` | reply/repost/like/share buttons + text content in the first cell |
| `testNoDuplicateCellsAfterTabSwitch` | cell count doesn't roughly double after Search↔Home (#0049 guard) |
| `testLikeButtonTapFlipsAccessibilityValue` | like value flips `not liked`↔`liked` within ~8 s (#0041/#0053 surface) |
| `testReplyButtonOpensComposerOrThread` | tapping reply presents a composer sheet or pushes `thread-view` |
| `testScreenshotHomeFeed` / `…Dark` | captures light + dark home-feed screenshots |

Note: SwiftUI renders the `post-cell` accessibility container as a generic
element, not a UIKit `.cell`, so the suite matches it via
`descendants(matching: .any).matching(identifier: "post-cell")` rather than
`app.cells["post-cell"]` — same identifier, type-agnostic query.

## Verification

```
cd /Users/brennan/Developer/Bluesky/Bluesky-SwiftUI
xcodebuild test -workspace Bluesky.xcworkspace -scheme "Bluesky (Beta)" -testPlan Screenshots \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:Bluesky-SwiftUIUITests/HomeFeedUITests \
  BLUESKY_TEST_HANDLE='…' BLUESKY_TEST_PASSWORD='…'
```

Observed (iPhone 17 simulator, live creds): **7 tests executed, 0 failures**.
All ran by name and passed:
`testFeedLoadsAtLeastOnePostCell`, `testPostCellContainsActionRow`,
`testNoDuplicateCellsAfterTabSwitch`, `testLikeButtonTapFlipsAccessibilityValue`,
`testReplyButtonOpensComposerOrThread`, `testScreenshotHomeFeed`,
`testScreenshotHomeFeedDark`. `** TEST EXECUTE SUCCEEDED **`.

## Files changed

- `Bluesky-SwiftUI/Bluesky-SwiftUIUITests/HomeFeedUITests.swift` — new test class (commit `f54bf2e`).
- `BlueskyKit/Sources/BlueskyUI/PostCard.swift` — post-cell + action-button accessibility identifiers / values (commit `76ec275`).
- `BlueskyKit/Sources/BlueskyFeed/ThreadView.swift` — `thread-view` identifier (commit `76ec275`).

## Gotchas

- The like-flip test taps a *real* like against the live account, so it
  mutates server state; it polls up to ~8 s for the round-trip rather than
  asserting an instantaneous flip.
- The duplicate-cell check uses a "did not roughly double" guard (lazy
  rendering makes an exact count brittle), which is enough to catch the
  #0049 double-view-model failure mode.
