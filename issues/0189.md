# 0189 — macOS UI test suite: profile screen (own and others)

| | |
|---|---|
| **Status** | open |
| **Module** | Tests / BlueskyProfile |
| **Platform** | macOS |
| **First seen** | 2026-05-25 |

## Description

macOS mirror of #0178. Run/adapt the profile coverage on **macOS**: own profile header (avatar, name, handle, counts, Edit affordance, post tabs) and another user's profile (Follow button instead of Edit, reached via a feed author click). Note the macOS profile chrome differs — the #0178 iOS suite skipped a back-button test because the iPhone compact profile hides the nav bar; macOS will have its own navigation affordances to assert instead.

## Scope

- Verify on `platform=macOS`, reusing `profile-screen` / `profile-*` / `post-author-avatar` identifiers from #0178.
- Adapt own-profile entry (`selectTab(.profile)`) and other-profile navigation for the macOS shell; assert macOS-appropriate back/column navigation.

## Verification

`xcodebuild test -scheme "Bluesky (Beta)" -testPlan Screenshots -destination 'platform=macOS' …` runs the macOS profile tests green.

## Notes

- Mirrors #0178. Blocked by #0186.
