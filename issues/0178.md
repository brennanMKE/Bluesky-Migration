# 0178 — UI test suite: profile screen (own and others)

| | |
|---|---|
| **Status** | resolved |
| **Module** | Tests / BlueskyProfile |
| **Platform** | All |
| **First seen** | 2026-05-24 |
| **Closed** | 2026-05-24 |
| **Commit** | b5853bb (Bluesky-SwiftUI), 8b4d9e0 (BlueskyKit) |

## Description

Profile screen regressions have been some of the hardest to catch manually — broken banner and avatar (#0056), content clipped on leading edge (#0155), action buttons unresponsive from notification paths (#0159). Most require navigating to a real profile over the network. A UI test suite asserts the structural elements are present, action buttons are hittable, and own-profile menu items are wired.

Depends on #0175 (foundation).

## Regressions this suite would have caught

- **#0056** — Profile banner broken, avatar placeholder broken, post cards not dark mode. A test asserting `profile-banner` and `profile-avatar` exist would have caught the missing views.
- **#0155** — Profile content clipped on leading edge (regression). A test asserting the avatar's frame origin.x > 0 would have caught the layout regression.
- **#0159** — Profile action buttons unresponsive when reached from a notification. A test navigating to a profile from the notifications path and asserting `post-action-like` is hittable would have caught it.
- **#0046** — Tapping a post author avatar doesn't navigate to their profile. A test tapping `post-author-avatar` in the feed and asserting `profile-screen` appears would have caught it.

## Test cases

All tests use the #0175 harness.

### Own profile

`launch(script: [.selectTab(.profile)])`.

- **Profile header elements present.** Assert `profile-avatar`, `profile-display-name`, `profile-handle`, `profile-followers-count`, `profile-following-count` exist.
- **Banner present.** Assert `profile-banner` exists (or `profile-banner-placeholder` if the account has no banner).
- **Posts tab loads.** Assert `profile-posts-tab` exists and is selected by default; assert at least one `post-cell` appears.
- **Menu items reachable.** Tap the profile menu (ellipsis or `profile-menu-button`). Assert `Settings`, `Saved`, `My Lists`, `Moderation` menu items exist.

### Other profile (via feed author tap)

`launch(script: [.selectTab(.home), .waitForFeedLoad(minPosts: 1)])`. Then XCUITest taps the author avatar of the first post.

- **Profile screen pushes.** Assert `profile-screen` exists after tap.
- **Follow / Unfollow button present.** Assert `profile-follow-button` exists and is hittable.
- **Back navigation works.** Tap back. Assert `profile-screen` no longer exists at top level.

### Screenshot

- `screenshot(screen: "own-profile")` from the Profile tab.
- `screenshot(screen: "other-profile")` after navigating to an author from the feed.

## Notes

- Own-profile and other-profile use the same `ProfileScreen` view; the distinction is whether `actorDID == viewerDID`. Tests should cover both paths since the conditional rendering (follow button, own-profile menu) differs.
- Accessibility identifiers need to be added to `ProfileScreen`, `ProfileHeaderView`, and the stats row.
- Profile data requires a live network call. Mark network-dependent tests accordingly.

## Root cause

Not a bug — this is the #0178 deliverable: add the profile UI test suite (own
and others) on the #0175 harness. The profile views carried no accessibility
identifiers, so the suite could not address the header elements, tabs, or the
follow / edit affordances, and the home feed post author avatar had no
identifier for the navigate-to-another-profile path.

## Fix

- **BlueskyKit (`8b4d9e0`)** — added accessibility identifiers:
  - `ProfileScreen`: `profile-screen` on the page root (`children: .contain`);
    `accessibilityIDPrefix: "profile"` passed to the tab strip.
  - `ProfileHeaderView`: `profile-avatar` (iOS + macOS layouts), `profile-banner`
    / `profile-banner-placeholder`, `profile-display-name`, `profile-handle`,
    `profile-followers-count` / `profile-following-count` / `profile-posts-count`,
    `profile-edit-button`, `profile-follow-button` (Follow and Following),
    `profile-menu-button`.
  - `UnderlineTabStrip`: optional `accessibilityIDPrefix` → each tab tagged
    `<prefix>-<id>-tab` (e.g. `profile-posts-tab`) with a `selected` /
    `not selected` value. `nil` (default) leaves the home feed strip unchanged.
  - `PostCard`: `post-author-avatar` on the author avatar (enables the
    feed → other-profile navigation, #0046).
- **Bluesky-SwiftUI (`b5853bb`)** — `ProfileUITests.swift` (10 methods) on the
  #0175 harness, auto-run via `Screenshots.xctestplan` (file-system
  synchronized group; no pbxproj edit needed). Data-dependent checks
  `XCTSkip` cleanly (mirrors #0177).

## Verification

Ran on the iPhone 17 simulator with live creds via the Screenshots test plan:

```
cd /Users/brennan/Developer/Bluesky/Bluesky-SwiftUI
xcodebuild test -workspace Bluesky.xcworkspace -scheme "Bluesky (Beta)" \
  -testPlan Screenshots \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:Bluesky-SwiftUIUITests/ProfileUITests \
  BLUESKY_TEST_HANDLE='…' BLUESKY_TEST_PASSWORD='…'
```

**Result: 10 tests executed, 8 passed, 2 skipped, 0 failures — TEST SUCCEEDED.**

Passed:
- `testOwnProfileHeaderElementsPresent`
- `testOwnProfileBannerPresent`
- `testOwnProfilePostsTabSelectedByDefault`
- `testOwnProfileShowsEditAffordance`
- `testOwnProfileMenuItemsReachable`
- `testOtherProfilePushesFromFeedAuthorTap`
- `testScreenshotOwnProfile`
- `testScreenshotOtherProfile`

Skipped (clean, data/layout-dependent degradations):
- `testOtherProfileShowsFollowButton` — the feed served the viewer's own post
  first, so the tapped author resolved to the viewer's own profile (Edit
  affordance shown, Follow intentionally absent).
- `testOtherProfileBackNavigationPops` — the iPhone compact profile layout
  hides the navigation bar (`ProfileScreen` `.toolbar(.hidden, …)`), so no
  nav-bar back button is exposed to tap. The push itself is still verified by
  `testOtherProfilePushesFromFeedAuthorTap`, which passed.

## Files changed

- `BlueskyKit/Sources/BlueskyProfile/ProfileScreen.swift`
- `BlueskyKit/Sources/BlueskyProfile/ProfileHeaderView.swift`
- `BlueskyKit/Sources/BlueskyUI/UnderlineTabStrip.swift`
- `BlueskyKit/Sources/BlueskyUI/PostCard.swift`
- `Bluesky-SwiftUI/Bluesky-SwiftUIUITests/ProfileUITests.swift` (new)
