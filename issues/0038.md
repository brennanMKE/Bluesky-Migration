# 0038 — Module 15 gate: remaining screens feature parity validation

| | |
|---|---|
| **Status** | open |
| **Module** | BlueskyLists / BlueskyFeed / BlueskyModeration / BlueskySettings |
| **Platform** | All |
| **First seen** | 2026-04-29 |

## Description

The Module 15 gate — confirm each remaining screen reaches feature parity with its React Native counterpart — has not been validated. Screens include: Lists, Starter Packs, Saved Feeds, Video Feed, Labeler Profile, App Passwords, Bookmarks.

## Steps to reproduce

1. **Lists** — Open Lists screen, create a list, add a member, view list feed.
2. **Starter Packs** — Open a starter pack, view members, tap Follow All.
3. **Saved Feeds** — Pin/unpin a feed, reorder, confirm order persists.
4. **Video Feed** — Scroll through the vertical video feed and confirm videos play.
5. **Labeler Profile** — Subscribe to a labeler and confirm subscription is saved.
6. **App Passwords** — Create an app password, copy it, then revoke it.
7. **Bookmarks** — Bookmark a post, open Bookmarks screen, confirm it appears, swipe to remove.

## Expected behavior

All screens function end-to-end with the live API and match the React Native feature set.

## Actual behavior

Not validated. All screens are implemented but untested against the live API.

## Incomplete reason

Validation gate — requires running the live app to confirm feature parity. Cannot be validated by code analysis alone.

## Notes

Bailed on automated resolution 2026-05-05. This issue is a multi-screen validation gate that requires hands-on testing with a live Bluesky session; build-time analysis alone cannot satisfy it.

### Pre-validated by build

- `swift build` in `../BlueskyKit` is clean (Build complete in 0.09s, no warnings).
- All seven target screens have implementations present in BlueskyKit:
  - **Lists** — `BlueskyLists/ListsStore.swift`, `ListsViewModel.swift`, list screens
  - **Starter Packs** — `BlueskyLists/StarterPackScreen.swift`, `StarterPackViewModel.swift`, `StarterPackCreateSheet.swift`
  - **Saved Feeds** — `BlueskyFeed/SavedFeedsScreen.swift`, `SavedFeedsStore.swift`, `SavedFeedsViewModel.swift`
  - **Video Feed** — `BlueskyFeed/VideoFeedView.swift`
  - **Labeler Profile** — `BlueskyModeration/LabelerProfileScreen.swift`, `LabelerProfileStore.swift`, `LabelerProfileViewModel.swift`
  - **App Passwords** — `BlueskySettings/AppPasswordsScreen.swift`, `AppPasswordsStore.swift`, `AppPasswordsViewModel.swift`
  - **Bookmarks** — `BlueskyFeed/BookmarksScreen.swift`, `BookmarksStore.swift`, `BookmarksViewModel.swift` (+ `BlueskyDataStore/BookmarkStore.swift`, `BookmarkedPost.swift`)

### What the user needs to verify (live session required)

Run `Bluesky-SwiftUI` against a real Bluesky account and walk each scenario from the Steps to reproduce. Items to confirm per screen:

1. **Lists** — Open Lists screen; create a new list (curation or moderation); add at least one member by handle; open the list to view its feed; confirm the feed loads posts from members.
2. **Starter Packs** — Open an existing starter pack (or create one via the create sheet); view the member list; tap Follow All; confirm follow relationships are created on the live account.
3. **Saved Feeds** — From Saved Feeds screen, pin a feed and unpin it; reorder pinned feeds via drag; force-quit and relaunch; confirm the new pin/order persisted to the server preferences.
4. **Video Feed** — Open the vertical Video Feed; scroll several items; confirm videos auto-play, audio toggles, and pagination loads more clips.
5. **Labeler Profile** — Open a labeler's profile (e.g. Bluesky Moderation Service); subscribe; back out; reopen; confirm subscription state is restored from server.
6. **App Passwords** — Open Settings -> App Passwords; create a new app password (with and without privileged-message scope); copy the displayed token once; revoke an existing app password; confirm both list-refresh after each action.
7. **Bookmarks** — On any post, bookmark via post action menu; open Bookmarks screen and confirm the post appears; swipe (or use context menu) to remove; confirm it disappears from the list and from the post action state.

### Concrete next steps

- Walk the seven scenarios above on a real device/build with a live account.
- File a fresh issue for any specific failure encountered (do not lump them under #0038).
- Once all seven pass, set #0038 Status to `resolved` with `Closed: <date>` and a short note pointing at the testing session; no commit hash required since this is a validation-only resolution.
