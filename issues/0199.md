# 0199 — macOS Feeds screen shows placeholder "Feed" rows: `#` button routes to the bare SavedFeedsScreen instead of MyFeedsScreen

| | |
|---|---|
| **Status** | in-progress |
| **Module** | BlueskyFeed / Bluesky-SwiftUI |
| **Platform** | macOS / iPadOS |
| **First seen** | 2026-06-11 |

## Description

On macOS, the `#` toolbar button pushes the bare-bones `SavedFeedsScreen`, which renders every feed-generator row with hardcoded placeholder text — title "Feed" and subtitle "Feed" — instead of real feed names, and shows an empty "Saved" section header. The rich `MyFeedsScreen` (RN `Feeds.tsx` parity: search, resolved feed names, discover section) already exists but is only wired up on iOS.

## Steps to reproduce

1. Launch the Mac app and sign in.
2. Click the `#` button at the trailing edge of the toolbar (or pick Feeds from the hamburger drawer).

## Expected behavior

The Feeds screen matches the RN app and the iPhone build: each pinned/saved feed shows its actual display name (e.g. Discover, Popular With Friends) with avatar/metadata, plus the search bar and "Discover new feeds" section that `MyFeedsScreen` already implements.

## Actual behavior

Every generator feed row reads "Feed / Feed" with a generic sparkles icon; only the timeline row ("Following / Timeline") looks right because it's special-cased. The "Saved" section renders as an empty header when all saved feeds are pinned.

## Attachments

![macOS My Feeds screen showing placeholder Feed/Feed rows and an empty Saved section](0199/screenshot.png)

## Notes

Root cause, from reading the code:

- `MainTabView.swift` ~line 1038: the macOS `#` toolbar button sets `showSavedFeeds = true`, whose `navigationDestination` (~line 1048) pushes `SavedFeedsScreen(network:cache:)`. The iPad regular-width `list.star` button (~line 1025) routes to the same place.
- `BlueskyKit/Sources/BlueskyFeed/SavedFeedsScreen.swift:122` — `displayName(for:)` is a stub: `"timeline" → "Following"`, `"list" → "List"`, anything else → literally `"Feed"`. The row subtitle is `feed.type.capitalized` (`SavedFeedsScreen.swift:98`), hence "Feed / Feed". The screen never calls `app.bsky.feed.getFeedGenerators` to hydrate names/avatars.
- iOS instead routes its `#` button through `showMyFeeds` → `MyFeedsScreen` (`MainTabView.swift` ~line 1055), which resolves `GeneratorView` metadata, has the search bar and Discover section, and handles taps into `CustomFeedTimelineView`.

Likely fix: route the macOS `#` button (and the drawer's Feeds row) to `MyFeedsScreen` with the same `onFeedTap`/`onSuggestedTap` wiring iOS uses, rather than teaching `SavedFeedsScreen` to hydrate metadata. Also decide whether `SavedFeedsScreen` is still reachable anywhere once that lands (iPad regular-width toolbar button is the other caller); if not, it may be removable — and its empty-"Saved"-section rendering goes away with it.

## Relation

- Builds on the macOS navigation work in [#0196](0196.md)–[#0198](0198.md) (the `#` button and drawer that expose this screen).

## Review

### Round 1 — 2026-06-11 — claude-opus-4-8

**Verdict: Approve**

The fix is correct, minimal, and complete. Verified against the issue and the review criteria:

- **No reachable path to `SavedFeedsScreen` remains.** Grep for `showSavedFeeds` across the app repo: zero results (state var, `resetTransientNavState` line, both toolbar buttons, the drawer's `onPushMyFeeds`, and the old `navigationDestination` are all redirected/removed). Remaining `SavedFeedsScreen` string hits in the app repo are two explanatory comments only. All three callers (macOS `#` button, iPad regular-width `list.star`, macOS drawer Feeds row) now set `showMyFeeds`, and the `#if os(iOS)` guard around the `MyFeedsScreen` destination was removed so it registers on both platforms.

- **`MyFeedsScreen` is macOS-safe.** Read end-to-end (`BlueskyKit/Sources/BlueskyFeed/MyFeedsScreen.swift`). The only iOS-flavored modifiers are exactly the three the implementer claimed, all already `#if os(iOS)`-guarded: `navigationBarTitleDisplayMode(.inline)` (66), `textInputAutocapitalization(.never)` (90), `swipeActions` (273–291). No `UIScreen`/`UIKit`/`UIPasteboard`/haptics/`EditButton`/list-style usage; the screen is `ScrollView` + `LazyVStack` + plain buttons + `AvatarView`. The ↑↓ reorder buttons are deliberately cross-platform. Verified by inspection, not trust.

- **Wiring parity holds.** The cross-platform `onFeedTap` closes the screen for `type == "timeline"` (→ Home) and otherwise sets `openCustomFeedURI`; `onSuggestedTap` sets `openCustomFeedURI`. The `CustomFeedTimelineView` destination (`navigationDestination(item: $openCustomFeedURI)`, MainTabView ~1079) is registered unconditionally (per the #0205 comment), so the macOS path resolves.

- **Implementer flags scrutinized.** (1) `resetTransientNavState()` now resets `showMyFeeds = false` (MainTabView:284) — programmatic tab switches won't leave a stale push. (2) The macOS drawer Feeds row routes through `pushAfterTabSwitch(tab: .home, push: onPushMyFeeds)` (DrawerView:215), which calls `onDismiss()` first (drawer closes), switches tab, then pushes after a 220ms delay — so the push lands after any tab-change-triggered reset and the screen is not covered by an open drawer. This wiring is unchanged from #0198; only the closure target moved. (3) `SavedFeedsScreen.swift` in BlueskyKit is now dead code (referenced only by its own `#Preview`s — confirmed by grep across both repos).

- **#0196–#0198 preserved.** The diff only changes closure bodies and deletes the orphaned destination/state; no toolbar items added or removed (trailing toolbar still only `#`), and the FAB / shell composer sheet are untouched.

- **Builds verified.** macOS build (`Bluesky (Beta)`, `platform=macOS`) exits 0 with no `error:` lines. BlueskyKit is clean on `main` with no changes, as the implementer stated.

**Required follow-up (not blocking this issue):** `SavedFeedsScreen` is now unreachable dead code. Leaving it in place is acceptable for this issue, but per the workflow an un-ticketed follow-up is not allowed — a follow-up ticket MUST be filed to delete `BlueskyKit/Sources/BlueskyFeed/SavedFeedsScreen.swift` (and its previews), or to document a deliberate reason to keep the `public` API.

## Work log

| Date | Role | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-06-11 | implement | claude-sonnet-4-6 | 1,979 | 10,592 | 2,466,481 | 66,188 | $1.15 |

**Total: $1.15**
