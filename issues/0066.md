# 0066 — Module 15 gate (iOS): remaining screens feature parity on iPhone

| | |
|---|---|
| **Status** | open |
| **Module** | BlueskyLists / BlueskyFeed / BlueskyModeration / BlueskySettings |
| **Platform** | iOS |
| **First seen** | 2026-05-05 |

## Description

Once the iOS build is restored (#0061), the Module 15 feature-parity gate must be re-run on a real iPhone. Most screens are platform-agnostic, but several have iOS-specific behavior that cannot be exercised on macOS — most notably the Video Feed (TikTok-style full-screen vertical scroll) and any screens that rely on iOS swipe gestures, the share sheet, or the system PHPicker.

This issue split off from #0038, which now focuses on the macOS gate.

## Steps to reproduce

1. Resolve #0061 so the iOS build is green.
2. Install the app on a registered iPhone (or run in iOS Simulator) and sign in.
3. Run the live checks below.

## Live checks (must all pass)

- **Lists** — Create a list; add a member; open the list feed. Confirm the iOS swipe-to-edit / swipe-to-delete on member rows behaves as expected.
- **Starter Packs** — Open a starter pack; view members; tap Follow All; confirm haptic feedback (if any) fires and follow relationships are created.
- **Saved Feeds** — Pin/unpin a feed; reorder via the iOS drag-handle / long-press; force-quit and relaunch; confirm order persists.
- **Video Feed** — Open the vertical Video Feed. Scroll several clips with the iOS rubber-band swipe; confirm clips auto-play with sound; tap to mute/unmute; confirm pagination loads more clips on scroll. Background and return to foreground; confirm playback resumes correctly.
- **Labeler Profile** — Open a labeler; subscribe; back-swipe out; reopen; confirm subscription state restored from server.
- **App Passwords** — Create an app password; tap-and-hold or long-press to copy the token (iOS clipboard semantics); revoke an existing password; confirm list refreshes.
- **Bookmarks** — Bookmark a post; open Bookmarks; confirm post appears; swipe-to-delete via iOS swipe action; confirm post disappears.
- **Share sheet integration** — On any post or list, tap Share; confirm the iOS share sheet (`UIActivityViewController`) appears with valid URL/text; share to a Notes pin; confirm the destination receives the right content.
- **Pull-to-refresh** — On Bookmarks, Saved Feeds, and Lists, pull down via the iOS rubber-band gesture; confirm refresh fires and indicator dismisses correctly.

## Expected behavior

All nine checks pass. iOS-specific surfaces (vertical video, swipe gestures, share sheet, rubber-band pull-to-refresh) behave correctly without regressions from the macOS-focused work.

## Actual behavior

Validated live on 2026-06-10 (see observations below): App Passwords and Bookmarks pass end-to-end; Lists passes its create/open/delete flows but with two defects (#0203, #0204); Saved Feeds, Video Feed, Labeler Profile, and Starter Packs fail (#0201, #0202, #0205, #0206).

## Live testing observations (2026-06-10)

Validated on the booted iPhone 17 simulator (iOS 26.5, `co.sstools.Bluesky.beta`, live `rnmigration.bsky.social` session) via a new automated gate suite, `Bluesky-SwiftUIUITests/RemainingScreensGateUITests` (read-only recon for preference-backed screens; fully reversed mutations elsewhere). Server-side effects were independently verified against the public AppView and the account's PDS (`jellybaby.us-east.host.bsky.network`) during the run.

| Gate check | Result | Evidence / defects |
|---|---|---|
| **Lists** — create, open detail, swipe-to-delete | ⚠️ partial | Create succeeds server-side but the hub keeps showing "No Lists" — the post-create `getLists` re-fetch races AppView indexing with no optimistic insert (**#0203**, reproduced twice). After relaunch the list appears ([hub](0066/lists-hub-after-relaunch.png)); detail renders header + Posts/People/About tabs ([detail](0066/list-detail.png), [people](0066/list-detail-people.png), [about](0066/list-detail-about.png)); swipe-to-delete works and the PDS confirmed record deletion within seconds ([after](0066/lists-after-delete.png)). "Add a member" is impossible — no member-management UI exists (**#0204**). |
| **Starter Packs** — open, members, Follow All | ❌ fail | Unreachable: `StarterPackScreen` only mounts from a `starterpack-joined` notification this account doesn't have; the create sheet/wizard has no callers; profiles have no Starter Packs tab (**#0206**). Follow All would not have been pressed regardless (mass-follow mutation policy). |
| **Saved Feeds** — pin/unpin, reorder, persistence | ❌ fail | My Feeds renders and Discover suggestions load ([screenshot](0066/myfeeds-discover.png)), but the account's `savedFeedsPrefV2` is empty ([screenshot](0066/myfeeds-empty-saved.png)) — wiped by earlier single-preference `putPreferences` writes (**#0201**). Pin/unpin/reorder were deliberately **not** exercised: every write path sends a single-pref body that would clobber the remaining server preferences. Re-run after #0201 is fixed. |
| **Video Feed** — vertical scroll, autoplay, pagination | ❌ fail | Unreachable: `VideoFeedView` has no entry point anywhere in the app (**#0205**). |
| **Labeler Profile** — subscribe, state restore | ❌ fail | Moderation → Advanced shows "No subscribed labelers" ([screenshot](0066/moderation-no-labelers.png)) — `labelersPref` wiped (#0201) — so the screen is unreachable on this account. Code reading shows `LabelerProfileStore` reads/writes subscription state via `savedFeeds` `type:"labeler"` items instead of `labelersPref`, disagreeing with the Moderation hub (**#0202**); subscribing live would have written a malformed item, so it was not tapped. |
| **App Passwords** — create, copy token, revoke, list refresh | ✅ pass | Create sheet → "App Password Created" alert shows the one-time token with Copy ([alert](0066/apppasswords-created-alert.png)); row appears ([row](0066/apppasswords-row.png)); swipe-to-Revoke removes it and the list refreshes to the empty state ([after](0066/apppasswords-after-revoke.png)). Fully reversed. |
| **Bookmarks** — add, view, remove | ✅ pass | Empty pre-flight ([initial](0066/bookmarks-empty-initial.png)); bookmark on first home post flips optimistically ([feed](0066/bookmark-added-feed.png)); post appears on Saved ([saved](0066/bookmarks-with-post.png)); removing via the card's bookmark toggle empties the screen ([after](0066/bookmarks-after-remove.png)); cold relaunch confirms server truth ([relaunch](0066/bookmarks-empty-relaunch.png)). Note: the Saved screen uses the card toggle for removal (matches bsky.app), not row swipe-to-delete as this issue's checklist guessed. |
| **Share sheet** | ➖ not run | Not driven this session (XCUITest cannot assert into `UIActivityViewController` reliably); post Share button behavior was covered by #0177's suite. |
| **Pull-to-refresh** | ⚠️ spot-checked | Exercised on the Lists hub as part of the delete verification; Bookmarks/Saved Feeds refresh not separately driven. |

### Mutation / cleanup ledger (all server-verified)

| Mutation | Reversal | Verified |
|---|---|---|
| List "UITest-0066 List" created 22:16 (run 1) | Swipe-deleted by the first continuation run at 22:19:14 | PDS `listRecords` = 0 |
| List "UITest-0066 List" created 22:28 (run 2) | Swipe-deleted 22:31:03 | PDS poller observed 1 → 0; AppView getLists = 0 |
| App password "UITest-0066" created 22:32 | Revoked via swipe ~30 s later | List refreshed to "No App Passwords" |
| Bookmark on first home-feed post 22:32 | Removed on Saved screen; cold relaunch shows empty | `app.bsky.bookmark` server state via relaunch |
| Account end state | — | 1 baseline post, 0 lists, 0 listitems, follows unchanged (6) |

**Defects filed:** #0201 (putPreferences namespace wipe — also explains the pre-wiped saved feeds/labelers found at recon), #0202 (labeler subscription stored in `savedFeeds`), #0203 (created list invisible in hub), #0204 (no list member management UI), #0205 (Video Feed unreachable), #0206 (starter packs unreachable). Gate stays open until those are fixed and the failing checks re-run green. The macOS half is #0038, out of scope here.

## Notes

- Video Feed is the highest-risk surface — its UX paradigm (full-screen vertical, auto-play, swipe-driven pagination) is iOS-native and may need iOS-specific code that was deprioritized while macOS was the primary target. Confirm before closing whether the macOS rendering used in #0038 is even meant to ship as the same screen, or whether iOS should have a distinct implementation.
- Reference: `Bluesky-ReactNative/src/screens/VideoFeed/`, `Bluesky-ReactNative/src/screens/Lists/`, etc.
- Blocked by: #0061 (iOS build audit).
- Related: #0038 (macOS Module 15 gate).
