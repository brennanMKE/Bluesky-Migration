# 0200 — Remove dead `SavedFeedsScreen` from BlueskyFeed (unreachable after #0199)

| | |
|---|---|
| **Status** | open |
| **Module** | BlueskyFeed |
| **Platform** | All |
| **First seen** | 2026-06-11 |

## Description

`BlueskyKit/Sources/BlueskyFeed/SavedFeedsScreen.swift` (and `SavedFeedsScreen`'s `#Preview` blocks) is unreachable dead code: [#0199](0199.md) redirected every caller — the macOS `#` toolbar button, the iPad regular-width `list.star` button, and the drawer's Feeds row — to `MyFeedsScreen`. Delete the file (with its previews), or document a deliberate reason to keep the `public` API. Filed as the required follow-up from #0199's review.

## Expected behavior

`SavedFeedsScreen.swift` is removed from BlueskyFeed (verify `SavedFeedsViewModel` is still used by `MyFeedsScreen` and keep it), or a written rationale for keeping the screen exists.

## Actual behavior

The file remains in the package, referenced only by its own previews; two explanatory comments in the app repo mention it.

## Relation

- Carved out of: [#0199](0199.md) — review round 1's required follow-up.
