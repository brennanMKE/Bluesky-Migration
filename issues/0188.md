# 0188 — macOS UI test suite: thread view and post action buttons

| | |
|---|---|
| **Status** | open |
| **Module** | Tests / BlueskyFeed |
| **Platform** | macOS |
| **First seen** | 2026-05-25 |

## Description

macOS mirror of #0177. Run/adapt the thread-view + post-action coverage on **macOS**: navigating into a thread, the focal post and replies render, and the action bar (like/repost/reply/share/menu) is present and clickable. Account for macOS push/column navigation rather than the iOS nav stack.

## Scope

- Verify on `platform=macOS`, reusing `thread-view` / `thread-focal-post` / `thread-reply-cell` / `thread-sort-menu` identifiers from #0177.
- Adapt the navigate-into-thread path (`tapFirstPost`) and back/return behavior for the macOS shell.

## Verification

`xcodebuild test -scheme "Bluesky (Beta)" -testPlan Screenshots -destination 'platform=macOS' …` runs the macOS thread tests green (data-dependent reply checks may skip cleanly).

## Notes

- Mirrors #0177. Blocked by #0186.
