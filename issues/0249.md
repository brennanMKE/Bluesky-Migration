# 0249 — Remove dead `SavedFeedsScreen` from BlueskyFeed (unreachable after #0248)

| | |
|---|---|
| **Status** | open |
| **Module** | BlueskyFeed |
| **Platform** | All |
| **First seen** | 2026-06-11 |

> _Re-homed from **#0200** (origin/main) during the 2026-07-26 history merge to avoid a number collision with the existing #0200. Commit messages, issue branches, and work logs that reference #0200 from that branch refer to this issue._

## Description

`BlueskyKit/Sources/BlueskyFeed/SavedFeedsScreen.swift` (and `SavedFeedsScreen`'s `#Preview` blocks) is unreachable dead code: [#0248](0248.md) redirected every caller — the macOS `#` toolbar button, the iPad regular-width `list.star` button, and the drawer's Feeds row — to `MyFeedsScreen`. Delete the file (with its previews), or document a deliberate reason to keep the `public` API. Filed as the required follow-up from #0248's review.

## Expected behavior

`SavedFeedsScreen.swift` is removed from BlueskyFeed (verify `SavedFeedsViewModel` is still used by `MyFeedsScreen` and keep it), or a written rationale for keeping the screen exists.

## Actual behavior

The file remains in the package, referenced only by its own previews; two explanatory comments in the app repo mention it.

## Relation

- Carved out of: [#0248](0248.md) — review round 1's required follow-up.
