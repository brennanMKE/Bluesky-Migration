# 0191 — macOS UI test suite: notifications (list, grouping, and tap routing)

| | |
|---|---|
| **Status** | open |
| **Module** | Tests / BlueskyNotifications |
| **Platform** | macOS |
| **First seen** | 2026-05-25 |

## Description

macOS mirror of #0180. Run/adapt the notifications coverage on **macOS**: the list loads, grouped rows render (reason text/icon, no raw reason codes), the filter strip is present/switchable, and clicking a notification routes to a thread or profile. Account for the macOS navigation shell.

## Scope

- Verify on `platform=macOS`, reusing `notifications-screen` / `notifications-filter-strip` / `notification-cell` / `notifications-group-header` identifiers from #0180.
- Adapt tab entry (`selectTab(.notifications)`) and the row-tap routing for macOS; data-dependent routing checks may skip cleanly when the account lacks a given notification kind.

## Verification

`xcodebuild test -scheme "Bluesky (Beta)" -testPlan Screenshots -destination 'platform=macOS' …` runs the macOS notifications tests green.

## Notes

- Mirrors #0180. Blocked by #0186.
