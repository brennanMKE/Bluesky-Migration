# 0012 — Module 2 gate: networking endpoint groups not live-tested

| | |
|---|---|
| **Status** | incomplete |
| **Module** | BlueskyNetworking / BlueskyCore |
| **Platform** | All |
| **First seen** | 2026-04-29 |

## Description

The Module 2 gate requires unit tests hitting `public.api.bsky.app` to validate every Codable lexicon group: feed, actor, notification, graph, repo, chat, moderation. No live network tests have been written or run.

## Steps to reproduce

1. Add integration tests to `BlueskyKitTests` (or a separate `BlueskyNetworkingTests` target) that hit `public.api.bsky.app` with a real bearer token.
2. Run `swift test`.
3. Check each endpoint group returns non-empty, decodable responses.

## Expected behavior

All endpoint groups decode without error against the live production API.

## Actual behavior

No live integration tests exist. Codable round-trip tests pass with synthetic data but real API shape may differ.

## Notes

Requires a test account access token or a way to inject credentials into the test environment. Consider a `--env` flag or `XCTestCase.continueAfterFailure` approach with skipped tests when credentials are absent.

## Incomplete reason

Cannot be validated by code analysis alone — requires running the app in Xcode against a live device or simulator. Marked incomplete until live testing is performed.
