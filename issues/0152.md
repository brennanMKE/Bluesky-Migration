# 0152 — Bookmarks fail to load: Codable decoding error on `bookmarks[0].uri`

| | |
|---|---|
| **Status** | open |
| **Module** | BlueskyCore / BlueskyFeed |
| **Platform** | All |
| **First seen** | 2026-05-07 |

## Description

Opening the Bookmarks screen surfaces a fatal decoding error and the list never renders:

> Response decoding failed: DecodingError.keyNotFound: Key 'uri' not found in keyed decoding container. Path: `bookmarks[0]`. Debug description: No value associated with key CodingKeys(stringValue: "uri", intValue: nil) ("uri").

The SwiftUI `BookmarkView` (or whatever decodes the `app.bsky.bookmark.getBookmarks` response items) expects `uri` directly on each bookmark item, but the wire shape doesn't carry `uri` at that level. The JSON likely nests the post under a different key (commonly `subject`, `item`, or `post`), so the AT-URI lives at `bookmarks[0].subject.uri` — not `bookmarks[0].uri`.

## Attachments

![Bookmarks fatal decoding error: uri not found at bookmarks[0]](error.png)

## Steps to reproduce

1. Sign in.
2. Tap the Saved / Bookmarks tab (or open from the sidebar drawer per #0150).
3. Observe the error alert; the list never loads.

## Expected behavior

Bookmarks load and render as a paginated list of `PostCard`s. The `BookmarkView` Codable shape matches the actual `app.bsky.bookmark.getBookmarks` response — typically:

```json
{
  "bookmarks": [
    {
      "subject": { "uri": "at://...", "cid": "..." },
      "createdAt": "2026-..."
    },
    ...
  ],
  "cursor": "..."
}
```

The Swift type should mirror that nesting (`subject: PostRef` or `subject: StrongRef`, with `uri` and `cid` inside).

## Actual behavior

JSON decode aborts at `bookmarks[0]` because the outer item carries no `uri` key. The screen shows the error alert and no rows.

## Notes

- File to fix: search `BlueskyCore` for `BookmarkView` / `GetBookmarksResponse` / `BookmarkItem`. Likely in `BlueskyKit/Sources/BlueskyCore/Bookmark.swift` (created during Module 15 — see Progress.md, BookmarksScreen + BookmarksViewModel landed 2026-04-25).
- Cross-check the lexicon against either RN's `state/queries/bookmarks.ts` (in `Bluesky-ReactNative/src/`) or the AT Proto lexicon definition for `app.bsky.bookmark.getBookmarks`.
- The fix is almost certainly: rename the field to `subject` (or whatever the actual key is) and parse the nested `uri`/`cid` from there. Add a `subject: PostRef` (or matching) struct.
- Once decoding works, also confirm `BookmarksScreen` rendering still maps to the right field path when handing items to `PostCard`.
- Add a small Codable round-trip test to `LexiconCodableTests.swift` for the corrected shape so a future field rename is caught at the test boundary.
- This blocks #0057 / #0060 follow-up work on the Saved Posts UX.
