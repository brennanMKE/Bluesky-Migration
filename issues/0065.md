# 0065 — Module 14 gate (iOS): settings persistence on iPhone

| | |
|---|---|
| **Status** | resolved |
| **Module** | BlueskySettings |
| **Platform** | iOS |
| **First seen** | 2026-05-05 |
| **Closed** | 2026-06-11 |

## Description

Once the iOS build is restored (#0061), the Module 14 settings-persistence gate must be re-run on a real iPhone. Most settings store via `UserDefaultsPreferencesStore` and are platform-agnostic, but the Appearance behavior diverges from macOS — on iOS the app does not yet defer to the system appearance the same way, and the Settings UI surfaces some controls that don't apply on macOS (haptics, in-app browser, etc.). Each persistence path needs to round-trip through a force-quit + relaunch cycle on iOS.

This issue split off from #0037, which now focuses on the macOS gate.

## Steps to reproduce

1. Resolve #0061 so the iOS build is green.
2. Install the app on a registered iPhone (or run in iOS Simulator) and sign in.
3. Run the live checks below.

## Live checks (must all pass)

- **iOS appearance.** Toggle iOS-wide appearance (Control Center → brightness long-press, or Settings → Display & Brightness → Light/Dark) and confirm Bluesky updates immediately. If iOS provides an in-app override picker, confirm the choice persists across relaunch and that "Use System" returns to the system setting.
- **Font size.** Drag the Font Size slider; force-quit Bluesky from the app switcher; relaunch; confirm the slider is still at the chosen value and text in the feed reflects it.
- **Notifications toggles.** Settings → Notifications: toggle Likes / Reposts / Follows / Mentions / Replies / Quotes. Force-quit and relaunch. Confirm each toggle persists.
- **Content & Media + Accessibility.** Toggle at least one option in each pane (e.g. autoplay video, reduce motion). Force-quit and relaunch. Confirm both survive.
- **Languages.** Change the post-language selection. Force-quit and relaunch. Confirm it persists.
- **Haptics (iOS-only).** If a haptics toggle exists, flip it; confirm haptic feedback on actions changes accordingly; relaunch and confirm the toggle state survived.
- **In-app browser preference (iOS-only).** If the "open links in app" toggle exists, flip it, tap an external link, confirm it opens in the configured surface (SFSafariViewController vs. system Safari); relaunch and confirm the toggle persists.

## Expected behavior

All seven checks pass. iOS-specific controls are present where appropriate and store correctly via `UserDefaultsPreferencesStore`. iOS appearance behavior matches the user expectation (follow system or honor in-app override).

## Actual behavior

Validated live on 2026-06-10 (see observations below): six of eight checks pass; the Dim theme variant and app-wide font scaling fail (#0199, #0200). Re-run on 2026-06-11 after both fixes landed: all checks pass (see "Gate re-run" below).

## Live testing observations (2026-06-10)

Validated on the booted iPhone 17 simulator (iOS 26.5, `co.sstools.Bluesky.beta`, live `rnmigration.bsky.social` session) via a new automated gate suite, `Bluesky-SwiftUIUITests/SettingsGateUITests`. Each test mutates a setting, asserts the immediate effect, force-quits (`app.terminate()`), cold-relaunches, asserts the persisted state, and **restores the original value** (including the server-side post language). All five tests pass:

```
testThemeImmediateEffectAndPersistence        passed (131.7s)
testFontSizeImmediateEffectAndPersistence     passed (48.3s)
testContentAndAccessibilityTogglesPersist     passed (60.5s)
testNotificationToggleSurvivesRelaunch        passed (37.3s)
testPrimaryPostLanguagePersists               passed (73.6s)
```

Pixel checks sample Home-feed screenshots against the #0040 ALF palettes (light bg `#FFFFFF`/`#F9FAFB`, dark bg `#000000`/`#111822`, dim bg `#151D28`/`#1C2736`).

| Gate check | Immediate effect | Survives relaunch | Verdict |
|---|---|---|---|
| iOS appearance — System mode follows OS light/dark | ✅ live flip via `simctl ui appearance`, no relaunch ([dark](0065/home-system-dark.png) / [light](0065/home-system-light.png)) | ✅ (`appearance.colorMode` default) | pass |
| Theme — Light / Dark color mode | ✅ ([light](0065/home-light.png), [dark](0065/home-dark-dark.png)) | ✅ picker selection + palette after relaunch ([dark+dim](0065/appearance-dark-dim-relaunch.png), [light](0065/appearance-light-relaunch.png)) | pass |
| Theme — **Dim** variant | ❌ feed/thread/profile render the **Dark** palette (`#000000`/`#111822`/`#526580`) instead of Dim (`#151D28`/…); only shell chrome (tab bar tint `#0F73FF`) goes dim ([evidence](0065/home-dark-dim.png) vs [dark](0065/home-dark-dark.png)) | ✅ `appearance.darkVariant` selection persists | **fail → #0199** |
| Font size (XS–XL tiers) | ⚠️ only the Appearance-screen preview scales ([XL](0065/appearance-fontsize-xl.png)); feed text is pixel-identical at M and XL ([XL feed](0065/home-fontsize-xl.png)) — `AppearanceStore.fontSize` has no consumers outside the preview | ✅ XL selected after relaunch ([screenshot](0065/appearance-fontsize-xl-relaunch.png)) | **fail → #0200** |
| Notifications toggles (Likes → Push) | ✅ toggle flips ([off](0065/notifications-likes-push-off.png)) | ✅ still off after relaunch ([screenshot](0065/notifications-likes-push-off-relaunch.png)); restored on | pass |
| Content & Media (Autoplay Videos) | ✅ ([off](0065/content-autoplay-off.png)) | ✅ ([relaunch](0065/content-autoplay-off-relaunch.png)); restored on | pass |
| Accessibility (Reduce Motion + Disable haptic feedback, iOS-only) | ✅ ([on](0065/accessibility-toggles-on.png)) | ✅ both on after relaunch ([screenshot](0065/accessibility-toggles-on-relaunch.png)); restored off | pass |
| Post languages (Primary post language en → es) | ✅ picker updates, `putPreferences` accepted (no error alert) ([Spanish](0065/languages-primary-spanish.png)) | ✅ Spanish after relaunch — server + local mirror round-trip ([screenshot](0065/languages-primary-spanish-relaunch.png)); **reverted to English** (verified in prefs plist: `settings.primaryPostLanguage = "en"`) | pass |

Persisted store verified directly after the run: `group.co.sstools.bluesky` plist shows every key back at its default (`colorMode=system`, `darkVariant=dim`, `fontSize=m`, `autoplayVideo=true`, `reduceMotion=false`, `disableHaptics=false`, `notifications.like.push=true`, `primaryPostLanguage="en"`).

Not exercised (noted for completeness, not gate-blocking per the minimum scope):

- **In-app browser behavior** — the `Open Links In-App` toggle exists and its sibling Accessibility toggles persist, but tapping an external link to confirm SFSafariViewController vs. Safari was not driven.
- **Haptic feel** — no haptic hardware on the simulator; only the toggle's persistence was validated.
- **App language picker** — local-only persistence works the same way as the post-language local mirror, but note nothing reads `settings.appLanguage` at startup (no `Bundle.preferredLocalizations` hookup); RN-parity scope for that is #0120's domain, not this gate ("post languages" is the gate item).

**Defects filed:** #0199 (Dim variant overridden by `.adaptiveBlueskyTheme()` on five screens), #0200 (font-size tier not applied app-wide). Gate stays open until both are fixed and the theme/font checks re-run green. The macOS half of this gate is #0037 (out of scope here).

## Gate re-run (2026-06-11)

Both blockers were fixed since the 2026-06-10 run — #0199 (BlueskyKit `33ef599` + `a450d9c`) and #0200 (BlueskyKit `7113cb3` + app `f4f988d`) — so the full gate suite was re-run against a fresh build on the same booted iPhone 17 simulator (`25FCA843-F98D-48C8-A679-CFABCBA96A78`, live `rnmigration.bsky.social` session). Build: `xcodebuild build -workspace Bluesky.xcworkspace -scheme "Bluesky (Beta)" -destination 'generic/platform=iOS Simulator'` (default signing), installed via `simctl install`. Test run:

```
xcodebuild test -workspace Bluesky.xcworkspace -scheme "Bluesky (Beta)" \
  -destination 'id=25FCA843-F98D-48C8-A679-CFABCBA96A78' \
  -only-testing:Bluesky-SwiftUIUITests/SettingsGateUITests
```

**Executed 5 tests, with 0 failures (0 unexpected) in 451.793 seconds — TEST SUCCEEDED:**

```
testContentAndAccessibilityTogglesPersist     passed (60.8s)
testFontSizeImmediateEffectAndPersistence     passed (75.2s)
testNotificationToggleSurvivesRelaunch        passed (37.3s)
testPrimaryPostLanguagePersists               passed (146.0s)
testThemeImmediateEffectAndPersistence        passed (132.5s)
```

The two previously failing legs (see the 2026-06-10 observations above) are confirmed fixed:

- **Dim variant (#0199):** the theme test now passes with the feed rendering the Dim palette. Pixel-sampled this run's captures (3x device px): Dim feed = `#151D28` at (100,1300) and (1100,1800), immediately and after relaunch ([screenshot](0065/home-dark-dim-rerun.png)); Dark = `#000000` and Light = `#FFFFFF` at the same points — no regression.
- **Font size (#0200):** the font test now asserts feed-level scaling (same label-matched first post must measure taller at XL than at M) and passes; the XL capture shows the first post's handle truncating and body wrapping onto more lines vs. M ([screenshot](0065/home-fontsize-xl-rerun.png)).

Post-run restore verified directly in the `group.co.sstools.bluesky` plist: `appearance.colorMode = "system"`, `appearance.darkVariant = "dim"`, `appearance.fontSize = "m"`, `settings.autoplayVideo = true`, `settings.reduceMotion = false`, `settings.disableHaptics = false`, `settings.notifications.like = {"email":false,"push":true,"include":"all"}`, `settings.primaryPostLanguage = "en"` — every key back at its default.

Still unexercised, as in the 2026-06-10 run (simulator limitations, not gate-blocking per the minimum scope): in-app-browser link behavior (toggle persistence validated, link-open surface not driven) and haptic feel (no haptic hardware on the simulator). The macOS twin of this gate remains #0037.

## Notes

- Reference for iOS-specific controls: `Bluesky-ReactNative/src/screens/Settings/`.
- Code under iOS gates may include `#if os(iOS)` blocks in `BlueskyKit/Sources/BlueskySettings/` — audit those after the build is green to confirm none have drifted.
- Reference commit for the macOS appearance simplification: BlueskyKit `93314f7`. iOS may need a different appearance strategy (in-app override + system default) since iOS users frequently want per-app dark mode.
- Blocked by: #0061 (iOS build audit).
- Related: #0037 (macOS settings gate).
