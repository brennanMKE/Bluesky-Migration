# 0193 — macOS UI test suite: composer (sheet, character count, and post submission)

| | |
|---|---|
| **Status** | open |
| **Module** | Tests / BlueskyComposer |
| **Platform** | macOS |
| **First seen** | 2026-05-25 |

## Description

macOS mirror of #0182. Run/adapt the composer coverage on **macOS**: the composer opens, typing updates the character counter, the Post button enable/disable transitions are correct, and (gated) submission works. Account for the macOS composer presentation (window/sheet) and keyboard semantics.

## Scope

- Verify on `platform=macOS`, reusing `composer-sheet` / `composer-text-editor` / `composer-char-count` / `composer-post-button` / `composer-cancel-button` identifiers from #0182.
- Keep the real-submit test gated behind `BLUESKY_ENABLE_POST_SUBMIT=1` (default-skip) so macOS runs don't post to the live test account.

## Verification

`xcodebuild test -scheme "Bluesky (Beta)" -testPlan Screenshots -destination 'platform=macOS' …` runs the macOS composer tests green (submit test skipped by default).

## Notes

- Mirrors #0182. Blocked by #0186.
