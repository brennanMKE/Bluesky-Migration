# 0190 — macOS UI test suite: search and discovery

| | |
|---|---|
| **Status** | open |
| **Module** | Tests / BlueskySearch |
| **Platform** | macOS |
| **First seen** | 2026-05-25 |

## Description

macOS mirror of #0179. Run/adapt the search + discovery coverage on **macOS**: the Search surface opens, the discovery/trending content shows, typing a query returns results, and switching result tabs works. Account for the macOS search entry point (sidebar/toolbar field) which differs from the iOS search tab.

## Scope

- Verify on `platform=macOS`, reusing `search-screen` / `search-field` / `search-tab-strip` / `search-account-result` identifiers from #0179.
- The result→navigation tests are gated on #0184 (search results aren't wired to push profiles/threads); keep them as clean skips until #0184 lands.

## Verification

`xcodebuild test -scheme "Bluesky (Beta)" -testPlan Screenshots -destination 'platform=macOS' …` runs the macOS search tests green.

## Notes

- Mirrors #0179. Blocked by #0186. Related: #0184.
