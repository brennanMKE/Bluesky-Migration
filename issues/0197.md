# 0197 — Composer media posts rejected by PDS: `BlobRef` encodes without `$type: "blob"`

| | |
|---|---|
| **Status** | resolved |
| **Module** | BlueskyCore / BlueskyComposer |
| **Platform** | All |
| **First seen** | 2026-06-10 |
| **Closed** | 2026-06-10 |
| **Commit** | BlueskyKit `d6dc4a3` |

## Description

Submitting any post that carries a blob embed fails server-side with an
`InvalidRequest` from the PDS. The blob reference the app writes into the
record is missing the required `$type: "blob"` discriminator, so lexicon
validation rejects the record. This breaks, at minimum:

- **Image posts** — `app.bsky.embed.images` (`$.record.embed.images[N].image`)
- **Link-card posts with a thumbnail** — `app.bsky.embed.external.thumb`
- **Video posts** — `app.bsky.embed.video.video` (same encode path; not live-confirmed because the failing image/link cases already burn the submit, but the code path is identical)
- **GIF posts** — `app.bsky.embed.external` with a `thumb` blob (same path)

Found while running the #0064 composer gate on the iPhone 17 simulator
(iOS 26.5): the gate's image post and link-card post both failed at submit
with this error; the plain-text + mention post (no blob) succeeded.

## Steps to reproduce

1. Sign in, open the composer.
2. Type any text, attach an image (or paste a URL and let the link card resolve a thumbnail).
3. Tap Post.

## Expected behavior

The blob upload succeeds (it does) and the post record references the
uploaded blob in the AT Proto blob shape:

```json
{ "$type": "blob", "ref": { "$link": "<cid>" }, "mimeType": "image/png", "size": 1234 }
```

RN round-trips the `BlobRef` returned by `uploadBlob` verbatim, `$type`
included, so its records validate.

## Actual behavior

Alert: **Post failed — “Invalid app.bsky.feed.post record: Expected blob value
type (got {"mimeType": ..., "ref": ..., ...}) at $.record.embed.images[0].image”**
(image post), and the same at `$.record.embed.external.thumb` (link card with
thumbnail). No post is created.

## Attachments

![Image post rejected at $.record.embed.images[0].image](post-failed-image-embed.png)
![Link-card post rejected at $.record.embed.external.thumb](post-failed-link-thumb.png)

## Notes

- Root cause: `BlueskyKit/Sources/BlueskyCore/Embed.swift` — `BlobRef.encode(to:)`
  (lines ~48–53) writes only `ref`, `mimeType`, and `size`; it never writes
  `$type: "blob"`. Decoding works (the decoder ignores the extra `$type` the
  server sends), so the bug only surfaces on the write path — which was never
  live-validated until the #0064 gate run.
- Fix shape: add `case type = "$type"` to `BlobRef.CodingKeys` and encode the
  constant `"blob"`, leaving the decoder lenient (don't require `$type` on
  reads — some legacy records carry the deprecated `{cid, mimeType}` shape).
- Blast radius beyond the composer: anything that re-encodes a `BlobRef` into
  a record write (e.g. profile avatar/banner updates via `putRecord`, if they
  reuse this type) has the same latent bug and should be checked when fixing.
- Blocks #0064 live checks: image post, alt-text round-trip, video post, and
  the link-card submit half.

## Root cause

`BlobRef.encode(to:)` in `BlueskyKit/Sources/BlueskyCore/Embed.swift` wrote
only `ref`, `mimeType`, and `size` — it never emitted the `$type: "blob"`
discriminator that AT Proto lexicon validation requires on every blob value
inside a record write. Decoding never surfaced the bug because the decoder
simply ignored the `$type` the server sends; the write path was first
live-exercised by the #0064 gate run.

Audit confirmed `BlobRef` is the **only** blob-shaped encoder in
`BlueskyCore`: `Embed` (images / external thumb / video / video captions),
`ListRecord.avatar` (Graph.swift), and `UploadBlobResponse` (Repo.swift) all
serialize through it, so the single fix covers every write path.
`ProfileRecord` (Feed.swift) intentionally carries no avatar/banner blobs
(documented round-trip limitation), so no sibling encoder bugs exist. No
other category of `$type` drift was found, so no new issue was filed.

## Fix

In `BlueskyKit/Sources/BlueskyCore/Embed.swift`:

- `BlobRef.encode(to:)` now always emits `"$type": "blob"` alongside
  `ref.$link` / `mimeType` / `size`.
- Decoding stays lenient: `$type` is never required on reads, and the
  deprecated legacy blob shape `{ "cid": "<cid>", "mimeType": "..." }`
  (no `ref`, no `size`) found in old records is now also accepted
  (`size` defaults to 0). Previously a legacy-shaped blob would have failed
  to decode at the missing `ref` key.

New `@Suite("BlobRef lexicon (#0197)")` in
`BlueskyKit/Tests/BlueskyKitTests/LexiconCodableTests.swift` (8 tests):
`$type: "blob"` asserted at `embed.images[0].image`, `embed.external.thumb`,
and `embed.video` in encoded `PostRecord` JSON; direct `BlobRef` encode shape;
round-trip; decode of the server shape (with `$type`), the no-`$type` shape,
and the legacy `{cid, mimeType}` shape.

## Verification

**Unit tests (required leg — ran):**

```
cd /Users/brennan/Developer/Bluesky/BlueskyKit && swift test
```

`✔ Suite "BlobRef lexicon (#0197)" passed` — full run:
`Test run with 95 tests in 17 suites passed` (87 pre-existing + 8 new, all
green).

**App build (ran):**

```
cd /Users/brennan/Developer/Bluesky/Bluesky-SwiftUI && xcodebuild build \
  -workspace Bluesky.xcworkspace -scheme "Bluesky (Beta)" \
  -destination 'generic/platform=iOS Simulator'
```

`** BUILD SUCCEEDED **`

**Live re-verification (optional leg — ran, on the booted iPhone 17
simulator `25FCA843-F98D-48C8-A679-CFABCBA96A78`, live account
`rnmigration.bsky.social`):**

1. Baseline via public AppView `getAuthorFeed`: exactly 1 post, no embeds.
2. Seeded a generated 640×480 gradient PNG via `xcrun simctl addmedia`.
3. Ran the single image-post UI test (rebuilds + installs with default
   signing; credentials flow through the `Screenshots.xctestplan`
   `$(BLUESKY_TEST_HANDLE)` build-setting expansion, NOT `TEST_RUNNER_`
   prefixed vars):

   ```
   xcodebuild test -workspace Bluesky.xcworkspace -scheme "Bluesky (Beta)" \
     -destination 'id=25FCA843-F98D-48C8-A679-CFABCBA96A78' \
     -only-testing:'Bluesky-SwiftUIUITests/ComposerGateUITests/test02ImagePostWithAltText' \
     BLUESKY_TEST_HANDLE='rnmigration.bsky.social' BLUESKY_TEST_PASSWORD='…'
   ```

   `test02ImagePostWithAltText passed (150.325 seconds)` — the submit that
   failed with the `Expected blob value type` alert before the fix now
   dismisses cleanly and the post appears on the own-profile feed.
4. AppView confirmed the record landed with the exact required shape:
   `"image": {"$type": "blob", "ref": {"$link": "bafkreieqz2ndvw4…"},
   "mimeType": "image/png", "size": 102936}` plus the alt text.
5. Cleanup: `ComposerGateCleanupTests/testDeleteMarkerPosts` passed
   (33.784 seconds); AppView re-check shows the account back to **exactly
   1 post** (the original developer-account post `3mmnqjqdzjs2s`).

## Files changed

- `BlueskyKit/Sources/BlueskyCore/Embed.swift` — `BlobRef` encode emits
  `$type: "blob"`; decoder accepts the legacy `{cid, mimeType}` shape.
- `BlueskyKit/Tests/BlueskyKitTests/LexiconCodableTests.swift` — new
  `BlobRef lexicon (#0197)` suite (8 tests).

## Gotchas

- `TEST_RUNNER_`-prefixed env vars do NOT reach the UI test runner with this
  scheme — the test plan (`Screenshots.xctestplan`) expands
  `$(BLUESKY_TEST_HANDLE)` / `$(BLUESKY_TEST_PASSWORD)` from **build
  settings**, so pass them as plain `xcodebuild` setting overrides.
- Placeholder credentials are fine for the live tests when the simulator
  already holds a signed-in keychain session (default signing preserves it).
- Unblocks the #0064 gate's image / alt-text / link-card-submit / video
  legs, and the latent `ListRecord.avatar` re-encode path used by list edit
  round-trips.
